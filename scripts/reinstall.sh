#!/bin/bash
# ╔══════════════════════════════════════════════════════════════════════════╗
# ║     CDN Portal — Clean Reinstall Script                                 ║
# ╚══════════════════════════════════════════════════════════════════════════╝
#
# Removes all old CDN installations and runs a fresh auto-install.
#
# Usage:
#   sudo CDN_NODE_NAME="Athi Community Network" CDN_ADMIN_PASSWORD="SecurePass123" bash reinstall.sh
#
# Or with curl (one-liner):
#   curl -sSL https://raw.githubusercontent.com/solomonitotia/cdn-raspberrypi/main/scripts/reinstall.sh | \
#     sudo CDN_NODE_NAME="Athi Community Network" CDN_ADMIN_PASSWORD="SecurePass123" bash

set -e

RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m'

log()   { echo -e "${GREEN}✓${NC} $1"; }
warn()  { echo -e "${YELLOW}⚠${NC} $1"; }
error() { echo -e "${RED}✗${NC} $1"; exit 1; }
info()  { echo -e "${CYAN}ℹ${NC} $1"; }

[ "$EUID" -ne 0 ] && error "Run as root: sudo bash $0"

echo -e "${CYAN}${BOLD}"
cat << "EOF"
  ╔════════════════════════════════════════════════════════╗
  ║                                                        ║
  ║     🧹  CDN Portal - Clean Reinstall                  ║
  ║                                                        ║
  ╚════════════════════════════════════════════════════════╝
EOF
echo -e "${NC}"

# ═══════════════════════════════════════════════════════════════════════════
# Step 1: Stop and remove old services
# ═══════════════════════════════════════════════════════════════════════════

echo -e "${CYAN}${BOLD}────────────────────────────────────────────────────────${NC}"
echo -e "${CYAN}${BOLD} Removing Old Installation${NC}"
echo -e "${CYAN}${BOLD}────────────────────────────────────────────────────────${NC}"
echo ""

for svc in cdn-portal content-admin athi-cn; do
    if systemctl is-active --quiet "$svc" 2>/dev/null; then
        info "Stopping service: $svc"
        systemctl stop "$svc"
    fi
    if systemctl is-enabled --quiet "$svc" 2>/dev/null; then
        systemctl disable "$svc" 2>/dev/null || true
    fi
    if [ -f "/etc/systemd/system/$svc.service" ]; then
        rm -f "/etc/systemd/system/$svc.service"
        info "Removed service file: $svc.service"
    fi
done

systemctl daemon-reload
log "Old services removed"

# ═══════════════════════════════════════════════════════════════════════════
# Step 2: Remove old installation directories
# ═══════════════════════════════════════════════════════════════════════════

for dir in /opt/cdn-portal /opt/athi-cn /opt/content-admin; do
    if [ -d "$dir" ]; then
        info "Removing $dir ..."
        rm -rf "$dir"
        log "Removed $dir"
    fi
done

# ═══════════════════════════════════════════════════════════════════════════
# Step 3: Remove old service users
# ═══════════════════════════════════════════════════════════════════════════

for user in cdnportal athi-cn contentadmin; do
    if id "$user" &>/dev/null; then
        userdel "$user" 2>/dev/null || true
        info "Removed user: $user"
    fi
done

log "Old installation cleaned up"
echo ""

# ═══════════════════════════════════════════════════════════════════════════
# Step 4: Run fresh auto-install
# ═══════════════════════════════════════════════════════════════════════════

echo -e "${CYAN}${BOLD}────────────────────────────────────────────────────────${NC}"
echo -e "${CYAN}${BOLD} Starting Fresh Installation${NC}"
echo -e "${CYAN}${BOLD}────────────────────────────────────────────────────────${NC}"
echo ""

# Download and run auto-install from a clean home directory
cd /root

curl -sSL https://raw.githubusercontent.com/solomonitotia/cdn-raspberrypi/main/scripts/auto-install.sh \
    -o /tmp/auto-install.sh

# Pass through all CDN_ environment variables
env $(env | grep "^CDN_") bash /tmp/auto-install.sh

rm -f /tmp/auto-install.sh
